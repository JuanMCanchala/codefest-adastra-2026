# Datos y artefactos pesados
data/raw/
data/processed/
# corpus de prueba descargado (PDFs de terceros, no redistribuir)
data/corpus/
# CORPUS OFICIAL DE ADL: 3 GB de documentos de terceros. No se redistribuye; el
# jurado ya lo tiene. Solo viajan las 50 consultas y el inventario, que son lo
# necesario para reproducir la entrega.
data/adl/corpus/
data/adl/Extracto_Preguntas_50_v2.pdf
OneDrive_*.zip
entrega/base_vectorial/
entrega_smoke/
entrega_bge/
entrega_multi/
entrega_corpus/
*.faiss
*.jsonl
!eval_interno/**/*.jsonl
!tests/**/*.jsonl
# resultados.jsonl ES el entregable principal (Seccion 1.4): tiene que viajar en
# el repositorio. La regla generica *.jsonl lo estaba excluyendo.
!entrega/resultados.jsonl
# Las 50 consultas de ADL, necesarias para reproducir la entrega.
!data/adl/queries.jsonl

# Modelos descargados
models/
.cache/
*.bin
*.safetensors

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.venv311/
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Entorno
.env
.DS_Store

# PDFs de contexto y documento oficial del reto (no redistribuir en repo publico)
Combinado-Anexos-Certificados.*
CODEFEST_2026-1.pdf
Preguntas Frecuentes.xlsx

# Integracion continua: GitHub rechaza el push de .github/workflows/ si el token
# OAuth no tiene el scope 'workflow'. El archivo existe en local y funciona; para
# publicarlo basta 'gh auth refresh -s workflow' y retirar esta linea.
.github/workflows/

# Auxiliares de LaTeX
*.aux
*.toc
*.out
*.synctex.gz
