# =============================================================================
#  CODEFEST AD ASTRA 2026 - Etapa 1
#  Verificado en Python 3.13.14 con GPU RTX 4060 Laptop (8 GB).
#
#  IMPORTANTE: instalar torch CUDA ANTES que el resto y comprobarlo DESPUES.
#  docling y gliner arrastran la build de CPU de torch y la sustituyen en
#  silencio; 'pip install torch==2.13.0' responde "already satisfied" aunque la
#  instalada sea +cpu. Tras instalar todo:
#      python -c "import torch; print(torch.__version__, torch.cuda.is_available())"
#      -> debe imprimir  2.13.0+cu126 True
#  Si no, reinstalar con:
#      pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cu126 \
#          --force-reinstall --no-deps
# =============================================================================

# --- Core / utilidades ---
numpy>=1.26
pyyaml>=6.0
tqdm>=4.66

# --- Segmentacion de oraciones ---
# El segmentador propio (basado en reglas) es el predeterminado: pysbd tardaba
# 8 horas sobre el corpus completo. Se conserva como alternativa verificable.
pysbd>=0.3.4

# --- Extraccion multiformato ---
pymupdf>=1.24                 # backend de PDF por defecto (rapido)
docling>=2.0                  # IBM, MIT. Mayor fidelidad en PDF, ~60 s/documento
trafilatura>=2.0              # HTML: limpieza de boilerplate
pandas>=2.1                   # CSV/XLSX
openpyxl>=3.1                 # XLSX
easyocr>=1.7                  # OCR (CRAFT + CRNN, no generativo) sobre GPU
mapbox-vector-tile>=2.2       # .pbf del corpus: son vector tiles, NO OpenStreetMap
# pyosmium>=3.6               # respaldo por si algun .pbf si fuera de OSM

# --- Encoders + indice vectorial ---
torch>=2.13                   # instalar la build CUDA (ver nota de arriba)
sentence-transformers>=5.0    # cross-encoder del reranking
FlagEmbedding>=1.2            # BGE-M3: denso + pesos lexicos en una sola pasada
transformers>=4.40
faiss-cpu>=1.8

# --- Grafo de conocimiento (bonus) ---
gliner>=0.2.5                 # NER multilingue zero-shot (MIT), arquitectura encoder
networkx>=3.2                 # grafo -> graphml

# --- Tests ---
pytest>=8.0
